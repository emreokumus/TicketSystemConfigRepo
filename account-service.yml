server:
  port: 8081

eureka:
  client:
    instance:
      hostname: ${EUREKA_SERVER_HOST:localhost}
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}

accountservice: 
  cassandra:
    contact:
      point: localhost
    port: 9042
    username: cassandra
    password: cassandra
    keyspace:
      name: ticketsystem
